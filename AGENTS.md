# AGENTS.md

## 基本规则

- 需要给出只读命令的时候给到一个框内，方便复制。
- 只读取完成当前任务所需的文件。
- 不要无差别扫描整个仓库。
- 修改前先确认相关实现和配置。
- 优先最小范围调查，信息不足时再扩大范围。
- 默认采用最小修改原则，只修改当前任务必须修改的内容。
- 不要为了优化或代码美观擅自重构无关代码。
- 不要修改与当前任务无关的文件。

## 文件操作

- 未经明确要求，不要删除、移动或重命名现有文件。
- 不要擅自去输出tmp，output,outputs等目录，保持代码干净
- 不要擅自修改目录结构。
- 创建新文件前先检查是否已有类似实现。

## 排障

- 先分析原因，再修改配置。
- 优先使用只读命令检查问题。
- 每次尽量只修改一个变量。
- 用户已经明确尝试失败的方法，不要再次重复推荐。

## 运维操作

- 删除、清理、覆盖、重启等操作必须先确认影响范围。
- 涉及生产环境时，不要擅自执行部署、删除、重启或数据库修改。
- Docker、Kubernetes、Jenkins 修改前先检查现有配置。
- 不要擅自执行 `docker system prune -a`、`kubectl delete` 等高风险命令。
- 高风险操作优先给出命令和影响说明，由用户决定是否执行。

## 代码与配置

- 保留现有文件名、函数名、接口和业务逻辑，除非明确要求修改。
- 不要虚构不存在的文件、函数、环境变量或配置。
- 修改配置后，如果支持语法检查，应进行review检查。
- 不要把密码、Token、Secret、AccessKey 写入代码仓库。

## 输出要求

- 回答直接，避免无关的大段解释。
- 如果问题已经定位，优先给出结论和解决方法。
- 修改完成后简要说明修改了哪些文件和内容。
- ## 输出格式

  - 默认只使用文字、Markdown 表格和纯文本线框图进行表达。
  - 页面布局、架构、流程、模块关系等内容优先使用 `│ ─ ┌ ┐ └ ┘ ├ ┤` 等字符绘制纯文本线框图。
  - 除非用户明确要求，否则不要生成 PDF、Word、Excel、PPT、图片或其他附件文件。
  - 能用纯文本表达的内容，不要转换成图片。